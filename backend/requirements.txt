fastapi
uvicorn[standard]
python-multipart
aiofiles
aiosqlite>=0.19.0
pillow
numpy>=1.26,<2
opencv-python>=4.8.0,<4.11
torch
torchvision
supervision>=0.18.0
huggingface_hub>=0.20.0
einops>=0.7.0
triton-windows; platform_system == "Windows"
transformers
accelerate
safetensors
httpx
psutil

# SAM 3.1 native support must be installed separately after the above. Run:
# git clone https://github.com/facebookresearch/sam3.git
# cd sam3
# pip install -e .
# cd ..
#
# Then request checkpoint access at:
# https://huggingface.co/facebook/sam3.1
# Then run: hf auth login
# Then run: python backend/scripts/download_sam3.py --version sam3.1
